#/backend/src/.env.example

#Configuración de la Base de Datos PostgreSQL para el backend
# Copia este archivo como ".env" y reemplaza los valores con los datos reales.

PG_DB=deauth_alerts              # Nombre de la base de datos
PG_USER=your_username            # Usuario de PostgreSQL
PG_PASSWORD=your_secure_password # Contraseña segura para PostgreSQL
PG_HOST=deauth_postgres          # Nombre del contenedor PostgreSQL en Docker
PG_PORT=5432                     # Puerto donde corre PostgreSQL (por defecto 5432)

# Configuración JWT para autenticación
JWT_SECRET_KEY=your_jwt_secret_key  # Clave secreta usada para firmar y verificar los tokens JWT
JWT_ALGORITHM=HS256                 # Algoritmo de firma utilizado (HS256 es uno de los más comunes)
JWT_EXPIRE_MINUTES=1440             # Duración del token en minutos (1440 = 24 horas)
