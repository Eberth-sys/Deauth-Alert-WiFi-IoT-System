# backend/src/requirements.txt
# Dependencias DIRECTAS pineadas (D2). Versiones resueltas desde una imagen
# limpia `python:3.11-slim-bookworm` (no `pip freeze` de un entorno personal).

# -------------------- Framework y servidor --------------------
fastapi==0.139.0            # FastAPI (core). Se quitó `fastapi[all]`: las extras que usa la app se declaran explícitas.
uvicorn[standard]==0.51.0   # Servidor ASGI + soporte WebSocket (websockets/httptools)

# -------------------- Base de datos --------------------
psycopg2-binary==2.9.12     # Driver PostgreSQL precompilado (sin build-essential/libpq-dev en la imagen)
sqlalchemy==2.0.51          # ORM

# -------------------- Variables de entorno --------------------
python-dotenv==1.2.2

# -------------------- Validación y serialización --------------------
pydantic==2.13.4
email-validator==2.3.0      # REQUERIDO por EmailStr (lo traía `fastapi[all]`; ahora explícito)

# -------------------- Seguridad --------------------
python-jose==3.5.0          # JWT (HS256)
passlib[bcrypt]==1.7.4      # Hashing de contraseñas
bcrypt==4.0.1               # PIN de compatibilidad con passlib 1.7.4 (bcrypt >=4.1 rompe su self-test)
