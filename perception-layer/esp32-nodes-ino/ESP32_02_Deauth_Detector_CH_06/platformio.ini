; platformio.ini minimo — nodo ESP32_02 (canal 6). Reutiliza ../pio_common.ini y el CSV compartido.
; Proyecto PlatformIO independiente; el main.ino no se mueve ni se modifica (Arduino IDE sigue funcionando).
[platformio]
src_dir = .
extra_configs = ../pio_common.ini

[env:esp32_02_ch06]
