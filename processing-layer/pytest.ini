[pytest]
# Añade el directorio de la capa de procesamiento a sys.path para que los tests
# resuelvan los imports "from src..." al ejecutar pytest desde processing-layer/.
pythonpath = .
testpaths = tests
